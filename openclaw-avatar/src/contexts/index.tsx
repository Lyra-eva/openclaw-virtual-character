/**
 * Contexts 统一导出
 */

export { 
  VoiceProvider, 
  useVoice, 
  useVoiceInitialized, 
  useVoiceState, 
  useVoiceError 
} from './VoiceContext';
export type { VoiceContextValue, VoiceProviderProps } from './VoiceContext';
